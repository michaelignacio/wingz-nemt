DEBUG=True
SECRET_KEY=your-secret-key-here-change-in-production

# Database Configuration - PostgreSQL for both dev and prod
# Development PostgreSQL (local) - uncomment when PostgreSQL is set up:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/wingz_nemt_dev

# Temporary SQLite for initial development (comment out when switching to PostgreSQL)
DATABASE_URL=sqlite:///db.sqlite3

# Production PostgreSQL - update with your production credentials:
# DATABASE_URL=postgresql://username:password@your-host:5432/wingz_nemt_prod
